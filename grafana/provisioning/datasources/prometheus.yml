apiVersion: 1

# Auto-wire the Prometheus datasource so the jr doesn't click through setup.
datasources:
  - name: Prometheus
    uid: prometheus # fixed uid so the provisioned dashboard can reference it
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
